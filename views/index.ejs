<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
    crossorigin="anonymous">
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>List notes</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark  bg-dark">
        <a class="navbar-brand" href="#">List To Do</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/add">Add<span class="sr-only">(current)</span></a>
            </li>
          </ul>
          <span class="navbar-text">
           <a href="/logout"> <i class="fa fa-sign-out" aria-hidden="true">Log Out</i></a>
          </span>
        </div>
      </nav>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Content</th>
            <th>Day_Create</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% notesList.forEach(note => { %>
        <tr>
          <td><%=note.name%></td>
          <td><%=note.content%></td>
          <td><%=note.date%></td>
            <td>
                <a href="/edit/<%=note._id%>"><i class="fa fa-pencil fa-lg" aria-hidden="true"></i></a>
                <a href="/delete/<%=note._id%>" onclick="return confirm('Are you sure to delete this note ?');"><i class="fa fa-trash fa-lg" aria-hidden="true"></i></a>
            </td>
        </tr>
  <% }); %>
    </tbody>
</table>
</body>
</html>